/**
 * Popover component event types
 */

export interface PopoverOpenEvent {
  // Dispatched via "hp-open" when the popover opens
}

export interface PopoverCloseEvent {
  // Dispatched via "hp-close" when the popover closes
}
