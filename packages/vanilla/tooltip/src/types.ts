/**
 * Tooltip component event types
 */

export interface TooltipOpenEvent {
  // Dispatched via "hp-open" when the tooltip becomes visible
}

export interface TooltipCloseEvent {
  // Dispatched via "hp-close" when the tooltip is hidden
}
